# TODO (build step: context): negation, temporality, experiencer detection via medspaCy


def apply_context(entities: list[dict], text: str) -> list[dict]:
    """Stub: pass entities through unchanged."""
    return entities
