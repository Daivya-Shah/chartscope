# TODO (build step: deid): integrate Presidio analyzer/anonymizer for PHI redaction


def deidentify(text: str) -> tuple[str, int]:
    """Stub: return de-identified text and redaction count."""
    return text, 0
