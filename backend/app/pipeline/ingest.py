# TODO (build step: ingest): load MTSamples CSV, Synthea bundles, validate input


def ingest_note(note_text: str) -> dict:
    """Stub: normalize and validate incoming clinical note text."""
    return {"text": note_text.strip(), "sections": []}
