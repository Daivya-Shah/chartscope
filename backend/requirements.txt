# Lightweight web stack
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pandas>=2.2.0
python-multipart>=0.0.9
httpx>=0.27.0

# Clinical NLP — ingest + de-identification (step 2)
spacy>=3.7.0,<3.9.0
medspacy>=1.3.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
pysbd==0.3.4

# Clinical NER (step 3) — HuggingFace transformers, no scispaCy
transformers>=4.40.0
torch>=2.2.0

# Testing
pytest>=8.2.0

# ---------------------------------------------------------------------------
# Training only — fine-tuning / NER benchmark (backend/training/)
# Not required to run the FastAPI server or pytest.
# ---------------------------------------------------------------------------
datasets>=2.19.0
seqeval>=1.2.2
evaluate>=0.4.0
accelerate>=0.30.0

# ---------------------------------------------------------------------------
# Heavier NLP stack — installed in later build steps
# ---------------------------------------------------------------------------
# scispacy          # deferred: nmslib fails on Windows
# fhir.resources
# hccinfhir
# scikit-learn

# spaCy models (install separately):
#   python -m spacy download en_core_web_sm
#   python -m spacy download en_core_web_lg   # Presidio PHI recall; fallback en_core_web_md
