# Backend URL for the Vite dev-server proxy (forwards /api/* to this host).
# Default: http://localhost:8000
VITE_API_PROXY=http://localhost:8000

# Optional: override the API base path used by axios (default /api via proxy).
# VITE_API_BASE=/api
